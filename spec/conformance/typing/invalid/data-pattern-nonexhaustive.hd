# test: constrained data patterns require a fallback arm

data User:
    age: i32
    active: bool

fn read(user: User) -> i32:
    match user:  # diagnostic: nonexhaustive-match
        User { age=1 } => 1
