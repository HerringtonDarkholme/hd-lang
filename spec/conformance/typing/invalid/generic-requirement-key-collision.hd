# expect-error: generic-requirement-key-collision
trait Repo[T]

fn choose[T, U](first: Repo[T], second: Repo[U]) -> void:
    $.with(Repo[T]=first):
        $.with(Repo[U]=second):
            pass

data User: pass

fn choose_row[T](user_repo: Repo[User]) -> void $ Repo[T]:
    $.with(Repo[User]=user_repo):
        pass
