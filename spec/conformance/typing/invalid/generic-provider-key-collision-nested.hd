# test: requirements / generic provider keys / collision

trait Repo[T]
fn choose[T, U](first: Repo[T], second: Repo[U]) -> void:
    $.with(Repo[T]=first):
        $.with(Repo[U]=second):  # diagnostic: generic-requirement-key-collision
            pass
