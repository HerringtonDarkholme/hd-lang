# test: an arm after an unguarded catch-all is unreachable

enum Flag:
    On(value: i32)
    Off

fn choose(flag: Flag) -> i32:
    match flag:
        _ => 1
        Flag.Off => 0  # diagnostic: unreachable-match-arm
