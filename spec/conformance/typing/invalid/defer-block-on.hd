use std.task.block_on

fn ready!() -> i32:
    42

fn wait_now() -> i32:
    let pending: mut Suspend[i32] = ready()
    block_on(pending)

fn invalid() -> void:
    defer:
        _ := wait_now()  # diagnostic: suspension-forbidden-context
    pass
