# test: a variant pattern cannot name a missing payload field

enum Choice:
    Pair(left: i32, right: i32)
fn bad(choice: Choice) -> i32:
    match choice:
        Choice.Pair(missing=value, right=other) => value  # diagnostic: unknown-data-field
