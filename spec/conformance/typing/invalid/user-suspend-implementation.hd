data Replay: pass

impl Suspend[i32] for Replay:  # diagnostic: sealed-trait-implementation
    fn poll(mut self, context: PollContext) -> Poll[i32]: Poll.Ready(1)
    fn cancel(mut self) -> void: pass
