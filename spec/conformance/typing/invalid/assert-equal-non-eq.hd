# expect-error: missing-partial-eq
use std.testing.assert_equal

data Error:
    message: string

test "result equality needs PartialEq":
    let actual: Result[i32, Error] = Ok(1)
    assert_equal(actual, Ok(1), reason="values match")
