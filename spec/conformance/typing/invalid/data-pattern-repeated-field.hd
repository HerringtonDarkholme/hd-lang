# test: data patterns reject duplicate fields

data User:
    age: i32
    active: bool

fn read(user: User) -> i32:
    match user:
        User { age, age=other } => age  # diagnostic: duplicate-data-pattern-field
