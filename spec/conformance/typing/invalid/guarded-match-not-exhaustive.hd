enum Status:
    Queued
    Running

fn label(status: Status, urgent: bool) -> string:
    match status:  # diagnostic: nonexhaustive-match
        Status.Queued if urgent => "urgent"
        Status.Running => "running"
