# test: functions / declarations / mutual recursion with omitted results

fn is_even(n: i32):  # diagnostic: recursive-function-needs-result-type
    if n == 0: true
    else: is_odd(n - 1)

fn is_odd(n: i32):
    if n == 0: false
    else: is_even(n - 1)

pub fn check() -> bool:
    is_even(4)
