# test: iterating mut list[User] yields readonly User elements; a mutable list root grants no element mutation
data User:
    name: string

fn rename_all(users: mut list[User]) -> void:
    for user in users:
        user.name = "Grace"  # diagnostic: readonly-root
