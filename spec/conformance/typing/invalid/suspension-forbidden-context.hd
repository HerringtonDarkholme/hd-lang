use std.task.block_on

fn ready!() -> i32: 42

fn wait_now() -> i32:
    let pending: mut Suspend[i32] = ready()
    block_on(pending)

fn invalid(value: i32 = wait_now()) -> i32:  # diagnostic: suspension-forbidden-context
    value
