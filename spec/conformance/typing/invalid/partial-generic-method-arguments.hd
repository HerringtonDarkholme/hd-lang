# expect-error: partial-generic-arguments
data Identity:
    name: string

impl Identity:
    fn pair[Left, Right](self, left: Left, right: Right) -> (Left, Right):
        (left, right)

identity := Identity { name: "identity" }
value := identity.pair[string]("left", 1)
