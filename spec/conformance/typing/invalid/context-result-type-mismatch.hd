# test: requirements / context errors / wrong context type

fn main() -> $.Context[Clock] $ Clock + Backup: $.context(Backup=$.use(Backup))  # diagnostic: type-mismatch

trait Clock

trait Backup < Clock:
    fn label(self) -> i32
