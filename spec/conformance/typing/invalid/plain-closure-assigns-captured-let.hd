# test: a plain fn closure must not assign captured let storage
fn counter() -> fn() -> i32:
    let count: i32 = 0
    fn() -> i32:
        count = count + 1  # diagnostic: mutable-capture-requires-mut-fn
        count
