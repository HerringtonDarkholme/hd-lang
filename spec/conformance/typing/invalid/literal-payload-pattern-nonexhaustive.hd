# test: literal payload pattern nonexhaustive

enum Expr:
    Scale(value: i32, factor: i32)
fn incomplete(expr: Expr) -> i32:
    match expr:  # diagnostic: nonexhaustive-match
        Expr.Scale(value, factor=2) => value
