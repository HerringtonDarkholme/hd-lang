# test: block on in defer

use std.task.block_on
fn ready!() -> i32: 42
fn main() -> void:
    let pending: mut Suspend[i32] = ready()
    defer:
        _ := block_on(pending)  # diagnostic: suspension-forbidden-context
