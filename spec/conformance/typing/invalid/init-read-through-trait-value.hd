data Box:
    value: i32

trait Reader:
    fn read(self) -> i32

let reader: Reader = Box { value: 3 }
dynamic := reader.read()  # diagnostic: top-level-read-before-initialization
later := 5

impl Reader for Box:
    fn read(self) -> i32:
        later
