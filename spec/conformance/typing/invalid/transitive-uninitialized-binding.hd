# expect-error: top-level-read-before-initialization
first := apply(first_name)
let names: list[string] = ["Ada"]

fn apply(callback: fn() -> string) -> string:
    callback()

fn first_name() -> string:
    names[0]
