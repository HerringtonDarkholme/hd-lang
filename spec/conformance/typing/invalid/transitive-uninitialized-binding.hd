first := apply(first_name)  # diagnostic: top-level-read-before-initialization
let names: list[string] = ["Ada"]

fn apply(callback: fn() -> string) -> string:
    callback()

fn first_name() -> string:
    names[0]
