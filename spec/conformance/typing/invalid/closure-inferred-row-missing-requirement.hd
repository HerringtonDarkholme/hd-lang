# test: closures / requirement inference / missing requirement

pub fn value() -> i32:
    reader := fn() -> i32:
        _ := $.use(Clock)
        42
    reader()  # diagnostic: missing-requirement

trait Clock
