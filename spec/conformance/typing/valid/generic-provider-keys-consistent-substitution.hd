# test: requirements / generic provider keys / consistent substitution

trait Pair[A, B]
fn safe[T](same: Pair[T, T], mixed: Pair[i32, string]) -> void:
    $.with(Pair[T, T]=same, Pair[i32, string]=mixed):
        pass
