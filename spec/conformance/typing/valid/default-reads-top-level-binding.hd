# test: parameter and data-field defaults may read top-level bindings
limit := 10
let counter: i32 = 0

fn current(value: i32 = limit, seen: i32 = counter) -> i32:
    value + seen

data Config:
    size: i32 = limit
