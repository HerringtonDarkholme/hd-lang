# test: functions / declarations / mutual recursion with one annotated result

fn is_even(n: i32) -> bool:
    if n == 0: true
    else: is_odd(n - 1)

fn is_odd(n: i32):
    if n == 0: false
    else: is_even(n - 1)

pub fn check() -> bool:
    is_even(4)
