fn call_with[Args..., R](f: fn(Args...) -> R, args: Args...) -> R:
    f(args...)

fn zip_apply[As..., Bs..., Rs...](
    pairs: ((As, Bs)...),
    funcs: fn(As, Bs) -> Rs...,
) -> (Rs...):
    panic("fixture does not execute zip_apply")

fn all![Ts...](tasks: mut Suspend[Ts]...) -> (Ts...):
    panic("fixture does not provide a scheduler")
