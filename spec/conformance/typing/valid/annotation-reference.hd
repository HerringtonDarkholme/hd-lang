data Validation: pass
data Validator: pass
data Node: pass

impl Annotation for Validation:
    type Info = Validator

impl TypeAnnotator for Validation:
    fn build(self, target: TypeShape) -> Validator:
        panic("fixture does not materialize")

annotate Validation for Node: pass

reference := Validation::annotation_ref(Node)
