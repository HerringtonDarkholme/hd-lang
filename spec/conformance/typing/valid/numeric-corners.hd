fn arithmetic_shift(value: i32) -> i32:
    value >> 1

fn same_width(left: f32, right: f32) -> f32:
    left + right

fn floating_power(base: f64, exponent: f64) -> f64:
    base ** exponent

fn minimum_remainder() -> i32:
    let minimum: i32 = -2147483648
    minimum % -1
