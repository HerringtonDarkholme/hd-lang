struct User:
    name: string

fn rename(user: mut User, name: string) -> void:
    user.name = name

fn example() -> string:
    let user: mut User = User { name: "Ada" }
    readonly := user
    rename(user, "Grace")
    readonly.name
