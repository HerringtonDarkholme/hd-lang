data Child:
    name: string

data Holder:
    get_child: fn() -> mut Child

fn recover(get_child: fn() -> mut Child) -> mut Child:
    holder := Holder { get_child: get_child }
    holder.get_child()
