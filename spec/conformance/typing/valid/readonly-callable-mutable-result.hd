data Child:
    name: string

data Holder:
    get_child: fn() -> mut Child

fn recover(child: mut Child) -> mut Child:
    holder := Holder { get_child: fn() -> mut Child: child }
    holder.get_child()
