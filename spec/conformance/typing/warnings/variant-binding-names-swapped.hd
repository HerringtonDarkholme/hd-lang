# test: variant binding names swapped

enum Pair:
    Values(left: i32, right: i32)
fn read(value: Pair) -> i32:
    match value:
        Pair.Values(right, left) => left - right  # warning: variant-binding-name-mismatch
