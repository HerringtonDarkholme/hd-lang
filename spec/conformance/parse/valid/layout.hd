# Blocks use indentation and may have same-line bodies.

fn greet(name: string) -> string:
    if name == "":
        "anonymous"
    else:
        "hello, " + name

fn one_line() -> void: pass

fn test() -> void: pass

value := greet(
    "Ada",
)

greeting := "你好"
