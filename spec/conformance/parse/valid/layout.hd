# Blocks use indentation and may have same-line bodies.

fn greet(name: string) -> string:
    if name == "":
        "anonymous"
    else:
        "hello, " + name

fn one_line() -> void: pass

fn test() -> void $ Console: println("hi")

value := greet(
    "Ada",
)

greeting := "你好"
pattern := r"\d+\s+\w+"
prompt := r"""first line
second line"""
message := """hello\n
world"""
welcome := "Hello, $greeting"
summary := "Length: ${greeting.len()}"
price := "Cost: \$5"
literal_template := r"$greeting ${greeting.len()}"
