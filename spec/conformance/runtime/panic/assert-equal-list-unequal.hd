use std.testing.assert_equal

test "lists with the same elements in a different order fail assert_equal":
    assert_equal([1, 2, 3], [1, 3, 2], reason="deliberately reordered list")  # panic: assertion-failed
