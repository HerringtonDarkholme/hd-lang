# test: removing a map entry inside a for loop invalidates the loop iterator
test "removal during map iteration panics":
    let values: mut map[string, i32] = {"a": 1, "b": 2}
    for key, value in values:  # panic: iterator-invalidated
        if key == "a" and value == 1:
            _ := values.remove("b")
