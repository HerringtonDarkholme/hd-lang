# test: block_on of a cancelled suspension reports suspension-invalid-state
use std.task.block_on

fn ready!() -> i32: 42

pub fn main() -> void:
    let pending: mut Suspend[i32] = ready()
    pending.cancel()
    _ := block_on(pending)  # panic: suspension-invalid-state
