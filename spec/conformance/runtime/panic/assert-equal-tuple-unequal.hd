use std.testing.assert_equal

test "tuples differing in one element fail assert_equal":
    assert_equal((1, "one", true), (1, "one", false), reason="deliberately unequal last element")  # panic: assertion-failed
