# test: bool match

use std.testing.assert_equal

fn choose(flag: bool) -> i32:
    match flag:
        true => 42
        false => 0

fn main() -> i32: choose(true) + choose(false)

test "main returns 42":
    assert_equal(main(), 42, reason="bool match")
