# test: suspending calls in binary expression

use std.testing.assert_equal

fn value!(input: i32) -> i32: input

fn main!() -> i32:
    value!(20) + value!(22)

test "main returns 42":
    assert_equal(main!(), 42, reason="suspending calls in binary expression")
