trait Database:
    fn get_user!(self, id: string) -> Result[User?, DbError]

struct User:
    id: string

struct DbError:
    message: string

fn load_user!(id: string) -> Result[User?, DbError] $ Database:
    db := $.use(Database)
    db.get_user!(id)
