# test: supertraits reject missing parent implementations

trait Parent

trait Child < Parent:
    fn value(self) -> i32

data Item: pass

impl Child for Item:  # diagnostic: missing-supertrait-implementation
    fn value(self) -> i32: 42
