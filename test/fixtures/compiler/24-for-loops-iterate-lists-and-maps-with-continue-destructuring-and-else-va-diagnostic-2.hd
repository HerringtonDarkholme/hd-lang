# test: for loops iterate lists and maps with continue, destructuring, and else values

fn bad(values: list[i32]) -> void:
    for left, right in values:  # diagnostic: for-binding-arity
        pass
