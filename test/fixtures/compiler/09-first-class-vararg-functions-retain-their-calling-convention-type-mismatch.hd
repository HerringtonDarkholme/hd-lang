# test: first-class vararg functions retain their calling convention

fn fixed(value: list[i32]) -> i32: value.len()
fn apply_callback(callback: fn(i32...) -> i32) -> i32: callback()
fn main() -> i32: apply_callback(fixed)  # diagnostic: type-mismatch
