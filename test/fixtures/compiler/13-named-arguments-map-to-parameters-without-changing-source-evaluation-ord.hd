# test: named arguments map to parameters without changing source evaluation order
# expect-result: main = 21

fn first!() -> i32: 1
fn second!() -> i32: 2
fn combine(first: i32, second: i32) -> i32: first * 10 + second
fn main!() -> i32:
    combine(second=first!(), first=second!())
