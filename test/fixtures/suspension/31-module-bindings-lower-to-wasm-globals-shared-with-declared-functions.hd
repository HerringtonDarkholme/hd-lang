# test: module bindings lower to Wasm globals shared with declared functions
# expect: accept

let count: i32 = 0
label := "requests"

fn increment() -> void:
    count = count + 1

fn value() -> i32:
    count

increment()
