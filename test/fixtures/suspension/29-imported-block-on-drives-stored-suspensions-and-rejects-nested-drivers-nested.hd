# test: imported block_on drives stored suspensions and rejects nested drivers

use std.task.block_on

fn ready!() -> i32: 42

fn helper() -> i32:
    let pending: mut Suspend[i32] = ready()
    block_on(pending)

fn main!() -> void:  # panic: suspension-nested-driver
    _ := helper()
