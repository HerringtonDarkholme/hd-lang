# test: cancellation unwinds child frames before parent cleanup
# expect: accept

fn wait!() -> void: pass

fn leaf!() -> void:
    defer:
        pass
    wait!()

fn middle!() -> void:
    defer:
        pass
    leaf!()

fn main!() -> void:
    defer:
        pass
    middle!()
