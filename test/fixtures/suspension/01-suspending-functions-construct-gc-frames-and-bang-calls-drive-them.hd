# test: suspending functions construct GC frames and bang calls drive them
# expect-result: main = 42

fn add_two!(value: i32) -> i32 $ Clock:
    _ := $.use(Clock)
    value + 2

fn main!() -> i32 $ Clock:
    add_two!(40)
