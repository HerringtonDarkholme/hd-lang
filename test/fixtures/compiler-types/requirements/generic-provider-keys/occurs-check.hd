# test: requirements / generic provider keys / occurs check
# expect: accept

trait Repo[T]
fn safe[T](plain: Repo[T], nested: Repo[T?]) -> void:
    $.with(Repo[T]=plain, Repo[T?]=nested):
        pass
