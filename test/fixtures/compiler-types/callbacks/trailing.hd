# test: callbacks / trailing
# expect-result: main = 42

fn apply(callback: fn() -> i32) -> i32: callback()
fn add(base: i32, callback: fn() -> i32) -> i32: base + callback()
fn main() -> i32:
    left := apply:
        40
    add(left):
        2
