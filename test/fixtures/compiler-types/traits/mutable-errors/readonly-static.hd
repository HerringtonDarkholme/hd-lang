# test: traits / mutable errors / readonly static

trait Add:
    fn add(mut self) -> void
data Counter:
    value: i32
impl Add for Counter:
    fn add(mut self) -> void: pass
fn invalid(value: Counter) -> void: value.add()  # diagnostic: mutable-receiver-required
