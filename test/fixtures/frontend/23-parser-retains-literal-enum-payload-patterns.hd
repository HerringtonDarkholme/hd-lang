# test: parser retains literal enum payload patterns
# expect: parse

fn evaluate(value: Expr) -> i32:
    match value:
        Expr.Scale(left, factor=2) => left * 2
        Expr.Scale(left, factor) => left * factor
