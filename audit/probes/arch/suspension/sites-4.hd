# probe 5.5: one frame with 4 sequential suspension sites
fn step!(value: i32) -> i32: value + 1
fn main!() -> i32:
    v0 := 0
    v1 := step!(v0)
    v2 := step!(v1)
    v3 := step!(v2)
    v4 := step!(v3)
    v4
