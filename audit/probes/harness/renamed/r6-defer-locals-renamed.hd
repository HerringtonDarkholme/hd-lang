# fixture-runtime-profile: pending-gate
# fixture-runtime-scenario: cancellation-cleanup
let flag: bool = false

pub trait Gate:
    fn wait!(self) -> void

fn step!() -> void $ Gate:
    defer:
        flag = true
    $.use(Gate).wait!()

pub fn main!() -> void $ Gate:
    step!()

pub fn cleanup_ran() -> bool:
    flag
