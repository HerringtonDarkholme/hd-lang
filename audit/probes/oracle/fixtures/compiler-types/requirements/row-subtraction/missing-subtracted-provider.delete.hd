# test: requirements / row subtraction / missing subtracted provider

fn drop_logger[r](callback: fn() -> i32 $ r) -> i32 $ (r - Logger):
fn read() -> i32 $ Logger: 42
fn main() -> i32: drop_logger(read)
