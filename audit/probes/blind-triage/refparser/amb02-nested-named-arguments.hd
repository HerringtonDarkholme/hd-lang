# test: AMB-02 inner call named arguments followed by an outer positional argument (spec 05 Calls)
# expect: parse
fn f(a: i32, b: i32) -> i32:
    a + b

fn g(x: i32, y: i32) -> i32:
    x + y

fn h() -> i32:
    g(f(a=1, b=2), 3)
