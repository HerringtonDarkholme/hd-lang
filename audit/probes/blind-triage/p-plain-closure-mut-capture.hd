# test: plain closure passing a captured mut value to a mut parameter (spec 07 Captures)
data User:
    name: string

fn rename(user: mut User) -> void:
    user.name = "renamed"

fn plain_closure(user: mut User) -> fn() -> void:
    fn() -> void:
        rename(user)  # diagnostic: mutable-capture-requires-mut-fn
