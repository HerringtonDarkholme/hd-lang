# test: readonly map[K, V] is covariant in V (spec 04 Variance)
# expect-result: probe = 1
data User:
    name: string

fn count(users: map[string, User]) -> i32:
    users.len()

pub fn probe() -> i32:
    let users: mut map[string, mut User] = {"k": User { name: "a" }}
    count(users)
