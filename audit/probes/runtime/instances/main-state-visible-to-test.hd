use std.testing.assert_equal

let value: i32 = 1

pub fn main() -> void:
    value = 2

test "main does not run in the test instance":
    assert_equal(value, 1, reason="tests use fresh instances; main is not part of initialization")
