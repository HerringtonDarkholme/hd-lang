# test: cancelled suspensions cannot be driven and readonly values cannot be cancelled

fn ready!() -> i32: 42
fn main!() -> i32:  # panic: suspension-invalid-state
    let pending: mut Suspend[i32] = ready()
    pending.cancel()
    pending!()
