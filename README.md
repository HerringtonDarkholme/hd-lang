# hd-lang

A statically typed, Python-indented language for human-reviewed AI-generated code, integrating effects, validation, contracts, property testing, tool definitions, sandboxed capabilities, resumable workflows, serializable closures, incremental computation, and observability into one compiler-checked model.
